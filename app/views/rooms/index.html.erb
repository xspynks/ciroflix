<%= render partial: "shared/header" %>


<p style="color: green"><%= notice %></p>

<h1>Salas</h1>

<div id="rooms">
  <% @rooms.each do |room| %>
    <%= render room %>
    <p>
      <%= link_to "Entrar nessa sala", room %>
    </p>
  <% end %>
</div>

<%= render partial: "shared/footer" %>